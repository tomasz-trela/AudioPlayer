package com.example.audioplayer.core.presentation

import androidx.compose.ui.graphics.Color

val md_primary = Color(0xFF607AFB)          // główny akcent – niebieski
val md_onPrimary = Color(0xFFFFFFFF)        // tekst/ikony na primary

val md_background = Color(0xFF0F1323)       // tło aplikacji w dark mode
val md_surface = Color(0xFF0F1323)          // powierzchnia kart / ekranów
val md_onBackground = Color(0xFFFFFFFF)     // główny tekst
val md_onSurface = Color(0xFFFFFFFF)        // tekst na powierzchni

val md_surfaceVariant = Color(0xFF1A1E30)   // lekko jaśniejsza powierzchnia
val md_onSurfaceVariant = Color(0xFFA1A1AA) // tekst drugiego planu (zinc-400)
val md_outline = Color(0xFFA1A1AA)          // kontury, divider

val md_error = Color(0xFFFF4D4F)            // czerwony błąd (fallback)
val md_onError = Color(0xFFFFFFFF)          // tekst na błędzie

val md_shadow = Color(0x66607AFB)           // cień primary/40
